[ dev ]
Proxyless = true
Debug = false
Threads = 5
minimal = true # Minimal logging: when true only `log.message` calls will be shown

ignore_types = [] # Optional list of challenge types to ignore entirely (e.g. ["IMAGE_LABEL_SINGLE_SELECT"]) 

ignore_questions = [] # Optional list of questions (prompt text substrings) to ignore. Case-insensitive substrings.
